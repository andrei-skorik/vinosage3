# VinoSage — pinned dependencies (Python 3.14+)

# LangChain stack
langchain==1.3.4
langchain-core==1.4.2
langchain-openai==1.2.2
langchain-community==0.4.2
langgraph==1.2.4
# Durable conversation state (SPEC step 9): PostgresSaver on Supabase.
# psycopg[binary] avoids needing libpq headers on Streamlit Cloud.
# NOTE: floors, not exact pins — after `pip install`, freeze the resolved
# versions here to match the project's pinning convention.
langgraph-checkpoint-postgres==3.1.0
psycopg[binary]==3.3.4
psycopg-pool==3.3.1

# Observability (optional at runtime — see LANGSMITH_* in .env.example)
langsmith==0.8.10

# LLM provider (OpenRouter via OpenAI-compatible API)
openai==2.36.0

# Supabase
supabase==2.31.0

# Streamlit UI
streamlit==1.58.0

# Pydantic v2
pydantic==2.13.4

# Fuzzy matching
rapidfuzz==3.14.5

# Config
python-dotenv==1.2.2

# Utilities
httpx==0.28.1
tenacity==9.1.4
numpy==2.4.6
pandas==3.0.3
